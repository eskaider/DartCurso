main() {
//Booleanos

  bool? isActive; //el signo"?" significa que puede ser Null

  bool isActivated = true;
  bool isNotActive = !isActivated;

  //"!"significa que es negado(al principio) ------- Al final este no se referira como Null

  print(isActive);
}
